@page "/home"
@using Database
@using ElectronNET.API
@using LiteDB
@inject ILocalStorageService _storage
@inject IDatabase db

<MudButton Variant="Variant.Filled" Color="Color.Error" OnClick="@(()=> db.DELETEWHOLEDB())">DELETE WHOLE DATABASE</MudButton>
<MudGrid>
    <MudItem xs="3">
        <MudTextField T="string" Label="Key" @bind-Value="@key"></MudTextField>
    </MudItem>
    <MudItem xs="3">
        <MudTextField T="string" Label="Value" @bind-Value="@value"></MudTextField>
    </MudItem>
    <MudButton Variant="Variant.Filled" Color="Color.Dark">Save</MudButton>
</MudGrid>

@code{

    string key = "";
    string value = "";

    List<object> objs = new List<object>();

    void Save()
    {
        db.Write("db",key,value);
    }

    void Load()
    {
        db.Read();
    }

}